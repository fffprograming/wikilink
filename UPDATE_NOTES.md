# WikiLinkGame 更新内容・公開手順（2026-09-21）

ローカルの改修と公開用ZIPの作成まで完了。本番へのアップロード、AdSenseでの再審査申請は未実施です。審査通知だけから不承認の具体的な原因は断定できず、この更新による承認を保証することはできません。

## 調査と変更

- トップに約7.6MBの問題データをインライン埋め込みしていたため、初期HTMLを19,421バイトに分離。全4万問のデータは開始時に取得し、通信失敗時には再試行できます。問題数・内容・順序は変更していません。初回プレイ時の約7.6MBの転送量自体は残ります。
- `learn.html`：実際の収録問題3問に、推理のヒント、解答、間違えやすい点、記事を確かめる手順を追加。ランキングや回答統計には送信しません。JavaScriptを無効にしても解説を開けます。
- ゲーム：直近20問の経路と正誤を端末内に保存・表示・削除。解答後にデータ取得時点と読み方を案内し、問題ID・経路を記入した報告メールを作れるようにしました。メールは利用者自身が送信します。
- `editorial.html`：自動生成・機械検証と独自の解説を区別し、判定の限界と問題報告の方法を明記。
- 説明の整合性：3モード、国・年代を経路に使わない現行ロジック、日替わりの重複可能性を反映。既存の運営者名や経歴は元の記載を維持しています。
- プライバシー：ランキングの名前・スコア・登録日時、直近20問の保存、通信情報の扱いを実装に合わせて説明。
- 自動生成の日付別答え一覧を `noindex,follow` にし、サイトマップから除外。過去の答え合わせとして公開・利用は継続します。これは検索入口の整理であり、AdSenseの審査を回避したり承認を保証したりする措置ではありません。新規生成でも同じ設定が維持されます。
- 既定の配布版では広告配信を停止し、空の広告枠を除去。既存のパブリッシャーIDは `google-adsense-account` メタタグと `ads.txt` に維持。任意で `--ad-file ad.html` を指定すると本文下に広告枠を生成できます。広告の自動配置設定はAdSense管理画面側で確認してください。

## 公開

デスクトップの `WikiLinkGame-release-20260921.zip` を展開し、**中のファイル・assets/・archive/を同じ階層のまま**現在のホスティング先の公開ルートへアップロードしてください。index.htmlだけの差し替えではゲームが起動しません。ZIPには公開用ファイルだけを収録し、Python、テスト、バックアップ、ダンプ、古いテンプレートは含めていません。

`WikiLinkGame-before-20260921.zip` は改修前バックアップです。公開サーバーにはアップロードしないでください。

既存サーバーに `_tmpl.html`、`_wlg_template.html`、`wikilink_sample.html`、`Claude outputs/` や問題バンクのバックアップを公開している場合は、公開対象から外してください。本更新はそれらの元ファイルを削除していません。ZIPを使った公開でも、旧ファイルを残す方式のホスティングでは既存ファイルの整理が別途必要です。

公開後は本番URLで、トップ、練習3問、本編の開始、1問の回答、振り返り、問い合わせ先、スマートフォン表示を確認。その後AdSenseの「サイト」から再審査を申請します。Search Consoleがある場合はサイトマップも再送信します。アクセス数や「記事を何本増やせば通る」といった未確認の数値条件は設定していません。

## 継続更新

実際に寄せられた報告に対応し、経路の分かりにくい問題を見直してください。今後解説を増やす場合は、同じ文章の量産ではなく、その問題固有の中継点と読み方を加えます。3問の解説は現在の記事の全リンクを再検証した記録ではなく、収録済みのスナップショットに基づくものです。

既存の `daily-archive.yml` はこのフォルダ直下にあり、この配置だけではGitHub Actionsの実行を確認できません。実際のリポジトリの `.github/workflows/` に置かれた設定と実行履歴を確認してください。既存アーカイブの最後の日付は9月6日のまま維持しています。新しいコードで `build_archive.py` を実行すれば、解説ページを含むサイトマップとnoindex設定を再生成します。過去のバンクを使う日付を誤って再生成しないでください。

再ビルド（標準Pythonで実行可能）：

```sh
python3 build_wikilinkgame.py --from-json index.questions.json --out index.html --site-url https://www.wikilink-game.com/ --stats-url https://wikilink-stats.top-of-the-fujii.workers.dev
python3 tests/verify_site.py
python3 build_release.py
```

`build_wikilinkgame.py` と新しい `site_assets.py` はセットで管理してください。ハッシュ付きassetsは再ビルド時に生成され、公開ZIPには現在のHTMLから参照されるものだけが入ります。

## 検証

- 公開HTMLの内部ファイルリンク、UTF-8、サイトマップとnoindexの整合性
- 4万問の元データとの完全一致
- Chromeで開始前のデータ未取得、10問完走、振り返り・削除、日替わり回答、サドンデス終了、報告リンク、共有IDの再現
- 解説3問の操作、375px幅の横はみ出しなし、読み込み失敗からの再試行、JavaScript無効時の解説
- ブラウザ検証では本番統計APIへの通信をモックし、テスト回答や名前を登録しない

ブラウザテストは `python3 -m http.server 8765 --bind 127.0.0.1` を起動してから `node tests/browser.cjs`（PlaywrightとChromeが必要）。

## 参照した公式方針

- [AdSenseのコンテンツとユーザー体験](https://support.google.com/adsense/answer/10015918)：独自の価値、明快なナビゲーション、重複したコンテンツの整理。
- [Googleパブリッシャー向けポリシー](https://support.google.com/adsense/answer/9335564?hl=ja)
- [手動による対策・内容の薄いコンテンツ](https://support.google.com/webmasters/answer/9044175#thin-content)
- [Googleウェブ検索のスパムに関するポリシー](https://support.google.com/publisherpolicies/answer/11035931)
